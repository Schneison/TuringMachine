<Window x:Class="TuringMachine.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:viewmodels="clr-namespace:TuringMachine.ViewModel" 
        d:DataContext="{d:DesignInstance Type=viewmodels:WindowViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <ItemsControl Height="20" VerticalAlignment="Top" Background="#FFCACACA" BorderBrush="#FFA2A2A2" BorderThickness="1">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>

                    <StackPanel Orientation="Horizontal" />

                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Items>
                <Button Content="File" />
                <Button Content="Edit" />
            </ItemsControl.Items>
        </ItemsControl>
        <Canvas x:Name="GraphView" Margin="0,52,0,0">
            <Rectangle>

            </Rectangle>
            <Ellipse   Width="50" Height="50" Fill="Blue" Canvas.Left="375" Canvas.Top="192" />
            <Line X1="10" Y1="10" X2="50" Y2="50" Stroke="Black" StrokeThickness="2" StrokeEndLineCap="Triangle"/>
            


        </Canvas>
    </Grid>
</Window>